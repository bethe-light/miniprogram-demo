import 'core-js/modules/es6.promise.js';
import url from './url';
import isServerRendering from './env';

export default ({
    url,
    uid: (!isServerRendering() && document && document.cookie) ? (/tt_webid=(\d+)/gi.test(document.cookie) ? RegExp.$1 : '') : '', // user_id
});
